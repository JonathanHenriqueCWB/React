import React from 'react'

function Primeiro() {
    const msg = 'Primeiro componente funcional com JSX (function componente)'
    return (
        <div>
            <h3>Primeiro Componente</h3>
            <p>{ msg }</p>
        </div>
    )
}

export default Primeiro